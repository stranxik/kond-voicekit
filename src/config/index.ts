/**
 * VoiceKit Configuration
 */

export {
  ENVIRONMENTS,
  ENDPOINTS,
  VOICE_PRESETS,
  DEFAULTS,
  getEnvironmentConfig,
  buildEndpointUrl,
  resolveVoiceId,
  validateSecureUrl,
} from "./defaults";

export type { EnvironmentConfig } from "./defaults";
